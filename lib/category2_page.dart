import 'package:flutter/material.dart';

class Category2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.black,
        title: Text('Category 2'),
      ),
      body: Center(
        child: Text('This is Category 2 page.'),
      ),
    );
  }
}
