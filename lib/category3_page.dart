import 'package:flutter/material.dart';

class Category3Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.black,
        title: Text('Category 3'),
      ),
      body: Center(
        child: Text('This is Category 3 page.'),
      ),
    );
  }
}
